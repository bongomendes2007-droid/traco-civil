spring.application.name=traco-api

# Supabase PostgreSQL Connection
spring.datasource.url=jdbc:postgresql://db.khpmbksseiwmaurxtxwk.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=PCtd91zxrhLoFqb3
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway (se estiver habilitado)
spring.flyway.enabled=false

# Server
server.port=8000

# JWT
app.jwt.secret=${JWT_SECRET:traco-civil-dev-secret-key-change-in-production}
app.jwt.expiration-ms=${JWT_EXPIRATION:86400000}

# Debug logging for RLS and Audit troubleshooting
logging.level.br.com.traco.api=DEBUG
logging.level.org.springframework.orm.jpa=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.jdbc.datasource=DEBUG